import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  base: '/dumi-demo-component',
  publicPath: '/dumi-demo-component/',
  themeConfig: {
    name: 'dumi-demo',
  },
  
});
